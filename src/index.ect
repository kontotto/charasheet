<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">

  <title>CharaSheet</title>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
  <link rel="stylesheet" href="http://code.jquery.com/ui/1.12.1/themes/base/jquery-ui.css">
  <link rel="stylesheet" href="/css/test.css">
  <script src="https://code.jquery.com/jquery-2.2.4.min.js"></script>
  <script src="https://code.jquery.com/ui/1.12.1/jquery-ui.min.js"></script>
  <script src="/js/test.js"></script>
</head>
<body>
  <div class="container">
    <% for panel in @data : %>
      <div class="panel <%= panel.pnl_color %>">
        <div class="panel-heading"><%= panel.name %></div>
        <div class="panel-body">
          <div class="row">
            <% for i in panel.list : %>
              <div class="col-xs-6 col-sm-4 col-md-3 col-lg-2 block-mb-sm">
                <button type="button" class="btn <%= panel.btn_color %> center-block btn-block skill" id="<%= panel.parent+i.id %>" value="<%= i.name %>" data-ini="<%= i.ini %>" data-parent="<%= panel.parent %>">
                  <%= i.name + " " + i.ini %>
                </button>
              </div>
            <% end %>
            <div class="col-xs-6 col-sm-4 col-md-3 col-lg-2 block-mb-sm">
              <button type="button" class="btn <%= panel.btn_color %> center-block btn-block">追加</button>
            </div>
          </div>
          <div class="row">
            <div id="<%= panel.parent %>_slider" class="col-xs-4 col-xs-offset-1">
            </div>
            <div class="col-xs-2 col-xs-offset-1" id="<%= panel.parent%>">
              hello!!
            </div>
            <div class="col-xs-1">
              <button class="btn <%= panel.btn_color %> center-block btn-block" id="<%= panel.parent %>_button" data-parent="<%=panel.parent%>" disabled>
                確定
              </button>
            </div>
          </div> 
        </div>
      </div>
    <% end %>
  </div>
</body>
</html>
